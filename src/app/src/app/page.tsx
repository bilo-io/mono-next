'use client'
import { Layout } from '@/components/Navigation/Layout';

export default function DashboardPage() {
    return (
        <Layout title={'Home'}>
            <h1>Welcome to the RBAC demo</h1>
        </Layout>
    );
}
